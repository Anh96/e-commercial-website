Install JSON
    st1: npm init -y;
    st2: npm i express

push code github
// update code
    - git add.
    -git commit -m "comment anthing"
    - git push -m
// đồng bộ code từ git về máy
    - git pull / git pull origin

//Create 1 web server by nodejs 
    b1. cài đặt nodejs
    b2. cài package json
    b3 . cài express.js

//Test server by node js
    - test port: vào terminal 
        + node .
    - run node server : node tenfile.js

//install json package cho moi du an su dung nodejs lam server va viet bang javascript
- npm init
//Create web server by express.js 
    - $ mkdir my-express-app // tao file
    - $ cd my-express-app   //di chuyen den file vua tao
    - $ npm init -y //cai json
    - $ npm install express // cai express
    - $ code 
    - $ code index.js
    // cài chính xác express vào json và đúng version mong muốn
        - npm install --save-exact express@4.17.1
// Loại trừ file không muốn update lên github
- vào file .gitignore viết tên file ko muốn upload vào
// trường hợp file đã lỡ up lên git thì giải thoát file đó ra khỏi git cache là xong, bằng 1 dòng lệnh:
 b1: git rm -r --cached /path/to/file_or_folder 
 => Ví dụ: git rm -r --cached node_module/
 b2: Từ bây giờ file đó không còn là của git cache nữa nên nó không thuộc quyền quản lý của git nữa và bây giờ .gitignore mới có tác dụng. 
 Theo lý thuyết là vậy nhưng nếu bạn cần reset lại hết project để .gitignore hoạt động đúng thì mình thường xóa bỏ hết file của git cache luôn?
    git rm -r --cached .
b3: git add . lại 


